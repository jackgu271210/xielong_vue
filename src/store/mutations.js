// import {
//     NEWS_DATA
// } from './mutation-types'

export default {
    shownewsdata(state, newsdata) {
        state.newsdata = newsdata
    },
    showprodata(state, prodata) {
        state.prodata = prodata
    }
}